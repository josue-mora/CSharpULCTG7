<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
  xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
  xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
  x:Class="LCSharpMBG7.Views.VehiculosAdmin"
  Title="Vehículos Admin">

  <Grid Padding="10">
    <VerticalStackLayout Spacing="15">

      <Button
        Text="＋ Nuevo Vehículo"
        BackgroundColor="Black"
        TextColor="White"
        CornerRadius="8"
        Clicked="OnCreateClicked" />

      <CollectionView
        x:Name="VehiclesList"
        ItemsUpdatingScrollMode="KeepScrollOffset">
        <CollectionView.ItemTemplate>
          <DataTemplate>
            <Frame
              CornerRadius="8"
              Margin="0,5"
              Padding="10"
              BorderColor="LightGray">
              
              <Frame.GestureRecognizers>
                <TapGestureRecognizer
                  Tapped="OnVehicleTapped" />
              </Frame.GestureRecognizers>

              <HorizontalStackLayout Spacing="10">
                <Image
                  Source="{Binding ImageCarousel}"
                  WidthRequest="60"
                  HeightRequest="40"
                  Aspect="AspectFill" />
                <VerticalStackLayout>
                  <Label
                    Text="{Binding Name}"
                    FontAttributes="Bold"
                    FontSize="18" />
                  <Label
                    Text="{Binding Model}"
                    FontSize="14"
                    TextColor="Gray" />
                </VerticalStackLayout>
              </HorizontalStackLayout>
            </Frame>
          </DataTemplate>
        </CollectionView.ItemTemplate>
      </CollectionView>

    </VerticalStackLayout>
  </Grid>
</ContentPage>
